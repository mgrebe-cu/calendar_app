<% provide(:title, @user.full_name) %>

<div class="row">
    <aside class="span4">
        <section>
            <%= render 'shared/event_form' %>
        </section>
    </aside>
    <div class="span8">
        <% if @user.get_event_count != 0 %>
            <h3>Events (<%= @user.get_event_count %>)</h3>
            <ol class="events">
                <%= render @events %>
            </ol>
        <% end %>
    </div>
</div>
