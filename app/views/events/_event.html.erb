<li>
    <span class="content">
        <%= event.date %>
        <br>
        <%= link_to event.title, edit_event_path(event), :remote => true, :class => 'edit_event_link' %>
        <% if !event.location.nil? && event.location.size != 0 %>
            at <%= event.location %>
        <% end %>
        <br>
        <% if event.all_day || event.start.nil? || event.end.nil? %>
            All day
        <% else %>
            <%= event.start.strftime('%I:%M %p') %> to <%= event.end.strftime('%I:%M %p') %>
        <% end %>
        <%= link_to "delete", event, method: :delete,
                            data: { confirm: 'You sure?' },
                            title: event.title %>

    </span>
</li>
